# Wishmax PRD Index

| PRD | Status | Branch | Source | Notes |
|---|---|---|---|---|
| PRD-01 Design System | Complete | `ralph/design-system` | `archive/2026-07-01-design-system/prd-design-system.md` | Foundation. `DESIGN.md` (repo root) is the visual source of truth (Partiful-derived, light/playful). Materialized into web + RN tokens, fonts, core components, and enforced in the agent loop. All 9 stories complete; QA passed. Archived to `archive/2026-07-01-design-system/`. |
| PRD-02 Landing Page | QA | `ralph/landing-page` | `tasks/prd-landing-page.md` | Marketing site. Next.js + TS + Tailwind, conversion-focused, light/white-canvas per `DESIGN.md`. Decisions locked (2026-07-01): pre-launch waitlist CTA behind a swappable store; pricing teaser (US-008) deferred. Depends on PRD-01. Scaffold live at `web/` (US-001 done) — consumes `design/tokens.css` + `design/fonts.css` via relative `@import` (single source of truth, no copies). US-002 done: sticky header + footer in the root layout with responsive mobile nav, ported PRD-01 Button. US-003 done: hero with hook headline + pain/promise subhead + above-fold CTA + PRD-01 Before/After proof card via next/image (ported Badge/FeedPhotoTile/BeforeAfterCard; self-authored swappable hero imagery in `web/public/hero/`). US-004 done: "How it works" 3-step section (`id="how-it-works"`) — Standard Content Cards with mono icons + numbered Badge + Inter-700 title + one sentence; single column → 3 columns responsive. US-005 done: transformation gallery (`id="examples"`) — 8 sample result tiles via the ported PRD-01 FeedPhotoTile with Travel/Gym/Professional/Night-out category chips, next/image lazy-loaded (no CLS), on the periwinkle wash; grid 2→3→4 columns; self-authored swappable gallery imagery in `web/public/gallery/`. US-006 done: benefits / pain-point section (`id="benefits"`) — four named pain points (bad lighting, no good photos, expensive photographers, low matches) mapped one-to-one to Wishmax outcomes as Standard Content Cards (strong-tone pain Badge + mono icon + Inter-700 outcome title + one brand-voice sentence), on the white canvas; grid 1→2→2 columns. US-007 done: social proof section (`id="social-proof"`) — three testimonial cards (5 ink-glyph stars + brand-voice quote + circular next/image avatar + name/handle), content structure ready for real quotes/metrics with an honest "illustrative pre-launch previews" caption; self-authored swappable avatar imagery in `web/public/testimonials/`; on the periwinkle wash; grid 1→3→3 columns. US-009 done: FAQ section (`id="faq"`) — accessible WAI-ARIA accordion (each question an `<h3><button aria-expanded aria-controls>`, each answer a `role="region"` panel `hidden` while collapsed; native-button keyboard toggle — Enter opens, Space closes) covering the five conversion objections (is it really my face, photo privacy/data handling, how long it takes, platforms iOS/Android, cost/cancellation); Standard Content Cards on the white canvas; single readable column at every width. US-010 done: primary CTA + waitlist email capture (`id="waitlist"`) — the closing CTA on the periwinkle wash captures an email via `POST /api/waitlist` behind a single swappable `saveWaitlistEmail` seam (v1 gitignored-JSONL placeholder store); two spam guards (hidden honeypot field + per-IP rate limit, both curl-verified); client + server validation with success/error states; the primary CTA target is centralized in `web/app/lib/cta.ts` so it flips to an App Store / TestFlight link at launch with a one-line change; form errors stay in the black/white system (no red). US-011 done: SEO, metadata & Open Graph — centralized SEO constants (`web/app/lib/site.ts`, env-driven `SITE_URL` via `NEXT_PUBLIC_SITE_URL`), root-layout `metadataBase` + canonical + full `openGraph`/`twitter` (`summary_large_image`); a designed 1200×630 OG/Twitter share card via `app/opengraph-image.tsx` (+ `twitter-image.tsx` re-export, byte-identical) rendered with `next/og` from `design/theme.ts` tokens (heavy-Inter headline — Satori can't parse the variable Space Grotesk TTF); `sitemap.xml` + `robots.txt` file conventions; semantic headings + alt re-audited. Verified via view-source (curl) + rendered PNG. US-012 done: analytics & conversion tracking — privacy-conscious Plausible (cookieless, no PII, no consent banner) behind a thin `track()` seam (`web/app/lib/analytics.ts`); loaded prod-only when `NEXT_PUBLIC_PLAUSIBLE_DOMAIN` is set (`Analytics.tsx` in the layout). Primary CTA clicks (`CtaLink` in header/hero/mobile-menu, reads the `PRIMARY_CTA` swap seam) fire a `CTA Click` event and waitlist signups fire a `Waitlist Signup` event from the success branch; dev logs events to the console with ZERO network noise; Do Not Track / GPC honored. Browser-verified (dev console + prod stub payloads + DNT suppression). US-013 done: responsive & performance pass — no horizontal overflow at 320/390/768/1024/1280 (all 7 sections present, no section shadow, fonts loaded), images optimized (next/image AVIF/WebP, −70% vs JPEG), Space Grotesk display font shrunk to woff2 (−64%), favicon added (`app/icon.svg` → best-practices 100). Lighthouse mobile (prod build): 80 / 96 / 100 / 100 (perf/a11y/BP/SEO) under default simulated Slow-4G with CLS 0 & TBT 0, and a perfect performance 100 on the real (unthrottled) connection — the < 90 throttled perf is a documented synthetic-throttling + self-hosted-variable-font gap. **All PRD-02 stories complete (US-008 pricing teaser deferred/out of scope) — ready for final QA.** |
| PRD-03 App MVP | Planned | `ralph/app-mvp` | `tasks/prd-app-mvp.md` | Expo React Native (iOS + Android) + backend proxy (auth, paywall, GPT-Image 2 / Nano Banana Pro). Depends on PRD-01. |

## Status Legend

- Planned: PRD is not yet started.
- In Progress: At least one story is being built.
- QA: Build is complete and under audit.
- Complete: QA passed and the work is merged or accepted.
