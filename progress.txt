# Ralph Progress Log

Started: 2026-07-01
Active PRD: PRD-02 Landing Page (tasks/prd-landing-page.md) — branch ralph/landing-page
Previous run archived to archive/2026-07-01-design-system/ (PRD-01 Design System, complete)
Depends on PRD-01: consumes design/tokens.css + design/fonts.css + the design/showcase component patterns.

## Codebase Patterns

### Design system (carried forward from PRD-01 — non-negotiable)
- DESIGN.md (repo root) is the visual source of truth; read it before ANY UI work, every Ralph loop. Consume tokens, never hardcode color/type/spacing/radius/shadow. Web = design/tokens.css (Tailwind v4 @theme, use var(--token)); RN = design/theme.ts. Add missing values to DESIGN.md + both token files FIRST, then consume. Enforced in CLAUDE.md + AGENTS.md + the checklist.md reviewer gate — reject any change that hardcodes a visual value or diverges from DESIGN.md.
- Brand fonts are self-hosted under design/fonts/ (inter/, space-grotesk/, licenses). Web loads them via design/fonts.css (@font-face families "Inter" + "Space Grotesk"), which must be imported so the --font-* tokens resolve. NEVER hardcode a font family — the @font-face name IS the --font-display / --font-sans token value.
- Design invariants (verified against Partiful source, HOLDS TRUE): black/white action system (no colored buttons); gradients live on surfaces/overlays ONLY, never on buttons/interactive elements (verify CTA computed backgroundImage:none); max two type sizes per card; Space Grotesk display face only for hero headlines / largest titles (≥26px) — oversized numerals use Inter, not the display face; Warm Sand (#d9c58b) only for active states; status colors only on generation-state Badge dots, never decorative; selection = 2px midnight-ink outline ring (no reflow) or border-color swap; labels over photos = Badge `onPhoto` + --gradient-scrim overlay.
- Token-literal audit before marking any UI story done: `grep -rnE '#[0-9a-fA-F]{3,8}|font-family.*(inter|space|sans-serif)|font-size:.*px' <component/css files>`. Only allowed literals: 1px/2px border & focus-outline widths, opacity multipliers, transforms, transition timing, @keyframes. Everything visual else must be a token.

### Reusable components (built in PRD-01, live in design/showcase/src/components/)
- Available: Button (primary/ghost, onPhoto, loading), Chip/ChipGroup, Badge (soft/strong/onPhoto/status-dot), FeedPhotoTile (THE hero feed tile), BeforeAfterCard, Card, Sheet (modal/sheet overlay). PRD-02 US-003 (before/after) and US-005 (feed gallery) should reuse these patterns.
- These components are coupled to design/showcase/src/components.css (a token-only CSS file with real :hover/:active/:disabled pseudo-classes; inline styles can't express pseudo-states). Bringing a component into the Next.js site means porting the component + its slice of components.css, and importing design/tokens.css + design/fonts.css — decide the sharing mechanism in US-001 (copy vs shared package) and record it here.
- Photo components (FeedPhotoTile, BeforeAfterCard) take a REAL `src` (full-bleed <img object-fit:cover>). The showcase used gradient-token `placeholder` stand-ins because it has no licensed photos — but the LANDING PAGE requires real before/after photography and real testimonial avatars via next/image (US-003/005/007 forbid generic stock). Source real imagery; do not ship gradient placeholders as the hero proof.
- The Paywall component is APP-ONLY (app monetization) — it exists in the design system for PRD-03 but must NOT appear on the marketing website. (Aligns with US-008 pricing teaser being deferred/out of scope for v1.)

### Tooling (carried forward)
- Tailwind v4 consumes external tokens via `@import "../../tokens.css"` AFTER `@import "tailwindcss"` in the entry CSS — the @theme block merges into Tailwind's theme, no tailwind.config needed. Apply the same wiring in the Next.js app's global CSS (adjust the relative path to design/tokens.css + design/fonts.css).
- Node v25 / npm 11; no global tsc — use the local devDependency or npx. Tokens typecheck standalone: `cd design && npx -y -p typescript@latest tsc -p tsconfig.json`.
- No project-local browser: verify UI with a temp Playwright install (`cd /tmp && npm i playwright@1.58`) driving system Chrome (`chromium.launch({ channel: 'chrome' })`); screenshot the dev URL. Verify webfonts programmatically: after `document.fonts.ready`, check `document.fonts.check('14px Inter')` / `document.fonts.check('36px "Space Grotesk"')` and `[...document.fonts].map(f=>f.status)` ('loaded' vs 'error') — don't just eyeball the screenshot.
- design/showcase/ (Vite + React + TS + Tailwind v4) is the design-system runtime — NOT a product app. The landing site (US-001) is a NEW Next.js App Router project in its own clear web directory; add its install/dev/build/typecheck/lint/deploy commands + env vars to AGENTS.md's Unknown Stack section as the stack solidifies.

### Landing site (web/) — established US-001
- The marketing site is a Next.js 16 App Router + React 19 + TS + Tailwind v4 project at `web/`. Sharing mechanism for the design system is **direct relative `@import`, NOT copy/package**: `web/app/globals.css` does `@import "tailwindcss"; @import "../../design/fonts.css"; @import "../../design/tokens.css";`. This keeps `design/` the single source of truth — never copy token values into `web/`.
- **Site components live in `web/app/components/`** (US-002). Global chrome (`SiteHeader`, `SiteFooter`) is rendered in `web/app/layout.tsx` around `{children}`, so every page gets nav + footer for free — page files only render their own `<main>` sections.
- **Porting a PRD-01 component into web/**: copy the component TSX + its slice of `design/showcase/src/components.css` into `web/app/components/` (e.g. `chrome.css` holds the `.wm-btn` slice), and `@import` that CSS from `globals.css`. Inline `var()` styles can't express `:hover/:active/:focus-visible/:disabled`, so any component with interaction states needs its real CSS ported — inline `var()` is only for static one-off page styling. Button lives at `web/app/components/Button.tsx`; use `<Button>` for actions and the new `<ButtonLink>` (anchor, same `.wm-btn` classes) for CTAs that navigate (#waitlist today, App Store link later).
- **Interactive chrome is a client component** (`"use client"` in `SiteHeader` for the mobile-menu `useState` toggle); the footer stays a server component. Mobile nav swaps in via a `@media (max-width: 767px)` breakpoint in chrome.css (hide `.wm-nav`, show `.wm-nav-toggle`) — the JS only toggles the panel's presence.
- Nav/footer link targets are anchors to the not-yet-built sections (`#how-it-works`, `#examples`, `#faq`, `#waitlist`) + placeholder hrefs for legal/social — later stories fill in the section `id`s and the real waitlist/store target.
- CRITICAL wiring: `web/next.config.ts` sets `turbopack.root` to the repo root (one level up from `web/`). Without it, Turbopack won't resolve the parent-dir `../../design/*` CSS imports or bundle the `design/fonts/*` url()s. Keep it.
- Consume tokens in web components via `var(--token)` inline styles OR Tailwind utilities generated from the `@theme` block. Inline `var()` is clean and unambiguous and passes the token-literal audit — prefer it for one-off page styling; port `components.css` classes when bringing over PRD-01 components (pseudo-states need real CSS).
- Web quality gate: `cd web && npm run typecheck && npm run lint && npm run build`. Lint uses ESLint flat config importing `eslint-config-next/core-web-vitals` + `/typescript` directly — `next lint` is REMOVED in Next 16, and `FlatCompat` + eslint-config-next crashes on ESLint 9.39 ("circular structure to JSON"), so use the native flat exports.
- Browser verify: `cd web && npm run dev` (port 3100), then drive with the temp Playwright/system-Chrome pattern. Verify webfonts programmatically after `document.fonts.ready`: `document.fonts.check('14px Inter')` + `document.fonts.check('36px "Space Grotesk"')`. Note: Next dev injects `__nextjs-Geist*` faces for its error overlay — they report `unloaded` and are NOT applied to the page; ignore them.

### Landing site imagery & PRD-01 component port (established US-003)
- **Porting a PRD-01 photo component into web/**: the transformation-proof stack (`Badge`, `FeedPhotoTile`, `BeforeAfterCard`) now lives in `web/app/components/` with its CSS slice in `web/app/components/showcase.css` (@imported from globals.css). The web `FeedPhotoTile` differs from the showcase one: it renders the photo through **`next/image` with `fill`** (not a raw `<img>`), takes `priority` (above-the-fold LCP → preload) and `sizes` props, and is a plain `<div>` (not a `<button>` — the marketing tiles are non-interactive; drop the pressed/loading/forceState button states). Keep the gradient `placeholder` branch only for empty states.
- **next/image + fill**: parent must be `position: relative` (the `.wm-tile` already is). Set `object-fit: cover` (via `.wm-tile__img` class + inline style). `priority` on the tile emits `<link rel="preload" as="image">` with responsive `imageSrcSet`/`imageSizes` in the SSR HTML — that's the LCP proof. No-layout-shift comes for free from the fixed `aspect-ratio: 3/4` reserving space before load. Verify preload with `curl -s localhost:3100 | grep 'rel="preload"'`.
- **Hero imagery (real-photo gap)**: the landing page requires real before/after photography, but no licensed model photos exist yet (open question: imagery ownership). Stand-in assets are SELF-AUTHORED raster JPGs in `web/public/hero/` (before.jpg/after.jpg), generated by `web/scripts/gen-hero-images.mjs` (sharp renders an SVG studio-portrait → JPG; no stock/licensing risk). They are real photographic-register raster files served via next/image — NOT gradient placeholders — dimensioned 3:4 so licensed photos drop in at the same paths with zero code change. The hero shows an honest "Sample transformation — real member results at launch" caption. The generator's hex palette is baked image pixels, intentionally OUTSIDE the DESIGN.md token system (it's image content, not UI). US-005 gallery should reuse `web/scripts/gen-hero-images.mjs` as the pattern for its sample tiles until licensed imagery lands.
- **Landing sections**: page sections are components in `web/app/components/` (e.g. `Hero.tsx`) with a per-section CSS file (`hero.css`) @imported from globals.css; `page.tsx` composes them inside a single `<main>`. Section anchors the nav links target: hero CTAs point at `#waitlist` / `#examples` (built in later stories). Two-column landing layouts use a mobile-first single-column grid that becomes 2-col at the `@media (min-width: 768px)` breakpoint.
- **Feed gallery / lazy imagery (established US-005)**: the transformation gallery (`web/app/components/Gallery.tsx` + `gallery.css`, `id="examples"`) is a responsive grid of the ported `FeedPhotoTile` — reuse it, don't build a new tile. For gallery/below-fold tiles pass NO `priority` (next/image lazy-loads → `loading="lazy"`, and the tile is NOT preloaded in SSR — the opposite of the hero's `priority` LCP tiles); the fixed `aspect-ratio: 3/4` on `.wm-tile` reserves space so lazy decode causes no CLS/jank. Category framing = the tile's `category` prop → `Badge onPhoto` over the scrim. Gallery sits on `--gradient-sky-periwinkle` (surface wash) for the DESIGN.md alternating white/wash rhythm; `box-shadow: none` on the section. Verify lazy vs preload: `curl -s localhost:3100 | grep 'rel="preload"'` should NOT list the gallery images.
- **Gallery imagery**: `web/scripts/gen-gallery-images.mjs` reuses the `gen-hero-images.mjs` portrait pattern to emit `web/public/gallery/{category}-{n}.jpg` — one background palette + key light + wardrobe per category (travel/gym/professional/night-out), varied `people` presets so the set reads as different faces. Same rules as the hero generator: real .jpg served via next/image (NOT gradient placeholders), swappable to licensed photos at the same paths/3:4 with zero code change, palette hex = baked image pixels intentionally OUTSIDE the token system. Honest "Sample transformations" caption. US-007 testimonial avatars will hit the same imagery gap — reuse this generator pattern.

### Ralph workflow
- Use prd.json as execution state and progress.txt as persistent agent memory. Implement exactly one incomplete user story per Ralph loop invocation.
- After meaningful work: update this file, checklist.md, and PRDS.md (status), and add browser evidence for UI stories.

## Story Status (from prd.json)
- US-001 Next.js + Tailwind scaffold with design tokens — passes: true (web/ scaffolded, tokens+fonts wired, all checks green)
- US-002 Global layout, nav & footer — passes: true (sticky header + footer in root layout; responsive mobile nav; ported Button; all checks green)
- US-003 Hero section with hook + before/after visual — passes: true (Hero: hook headline + pain/promise subhead + above-fold CTA + BeforeAfterCard proof via next/image; ported Badge/FeedPhotoTile/BeforeAfterCard; all checks green)
- US-004 "How it works" section (3 steps) — passes: true (3 Standard Content Cards, mono icons + numbered Badge + Inter-700 title + one sentence; single col → 3 col; all checks green)
- US-005 Transformation gallery / sample feed showcase — passes: true (8 sample result tiles via ported FeedPhotoTile with category chips; next/image lazy-loaded, no CLS; periwinkle wash; grid 2→3→4; all checks green)
- US-006 Benefits / pain-point section — passes: false
- US-007 Social proof section — passes: false
- US-009 FAQ section — passes: false
- US-010 Primary CTA + waitlist email capture — passes: false
- US-011 SEO, metadata & Open Graph — passes: false
- US-012 Analytics & conversion tracking — passes: false
- US-013 Responsive & performance pass — passes: false

(US-008 Pricing teaser is DEFERRED / out of scope for v1 per the PRD and the app-only Paywall decision; intentionally not in prd.json. IDs are not renumbered, to preserve traceability.)

## Open Questions carried from the PRD
- Domain + legal pages (privacy/terms) ownership and copy — who provides?
- Hero: short product demo video vs stills-only for v1?
- Concrete waitlist provider (email list vs hosted DB) — deferred, not blocking v1; US-010 builds behind a `saveWaitlistEmail` seam with a documented placeholder store.

---
<!-- Append dated work entries below this line, format:
## [YYYY-MM-DD HH:MM] - [Story ID]
- Implemented:
- Files changed:
- Checks run:
- Browser evidence:
- Learnings for future iterations:
  - ...
---
-->

## [2026-07-01 15:40] - US-001
- Implemented: Next.js 16 (App Router, Turbopack) + React 19 + TypeScript + Tailwind v4 landing-site scaffold at `web/`, wired to the PRD-01 design system. `web/app/globals.css` imports Tailwind + `../../design/fonts.css` + `../../design/tokens.css` (single source of truth — no token copies); `next.config.ts` sets `turbopack.root` to the repo root so the parent-dir imports resolve. Root layout renders a tokenized white-canvas page (Space Grotesk display H1, Inter body, black-on-white CTA) with no Next boilerplate styling. Sharing mechanism decided: direct relative `@import` (recorded in AGENTS.md + Codebase Patterns).
- Files changed: web/package.json, web/tsconfig.json (Next auto-tuned jsx/include), web/next.config.ts, web/postcss.config.mjs, web/eslint.config.mjs, web/.gitignore, web/app/globals.css, web/app/layout.tsx, web/app/page.tsx; AGENTS.md (web stack + commands), PRDS.md (PRD-02 → In Progress), checklist.md (new), prd.json (US-001 passes: true), progress.txt.
- Checks run: `npm run typecheck` → no errors; `npm run lint` (eslint flat config) → exit 0; `npm run build` → compiled + 3 static pages generated, success. Token-literal audit on web/app → no hardcoded visual values.
- Browser evidence: Playwright + system Chrome at http://localhost:3100. After `document.fonts.ready`: `document.fonts.check('14px Inter')` = true, `document.fonts.check('36px "Space Grotesk"')` = true; both faces report `loaded`. Computed styles: body background rgb(255,255,255) (white canvas); H1 fontFamily "Space Grotesk", 48px, rgb(0,0,0); CTA backgroundColor rgb(0,0,0), backgroundImage `none` (design invariant — no gradient on buttons), color rgb(255,255,255), borderRadius 8px. Screenshots: /tmp/wishmax-mobile.png (390px) + /tmp/wishmax-desktop.png (1280px) — clean, on-brand, no framework boilerplate.
- Learnings for future iterations:
  - Turbopack root MUST be the repo root for `web/` to import the shared `design/*` CSS from the parent dir and bundle the self-hosted fonts — see Codebase Patterns.
  - `next lint` is removed in Next 16; use ESLint's native flat config importing `eslint-config-next/core-web-vitals` + `/typescript`. `FlatCompat` + eslint-config-next throws "circular structure to JSON" on ESLint 9.39 — avoid the compat path.
  - Next dev injects `__nextjs-Geist*` overlay fonts that report `unloaded`; they are not applied to the page — ignore them when auditing `document.fonts`.
  - Prefer `var(--token)` inline styles for one-off page styling (unambiguous, passes the token audit); reserve `components.css` porting for the shared PRD-01 components that need real pseudo-state CSS.
---

## [2026-07-01 17:10] - US-002
- Implemented: Global site chrome (sticky header + footer) rendered in the root layout so every page inherits nav + footer. `SiteHeader` (client component): Wishmax wordmark, desktop nav row (How it works / Examples / FAQ) + black "Get my photos" CTA, collapsing to a hamburger-toggled stacked mobile menu ≤767px (toggle sets `aria-expanded`/`aria-controls`, closes on link click). `SiteFooter` (server component): brand + tagline and Product / Company / Social link columns (Privacy, Terms, Contact mailto, Instagram/TikTok/X) + legal strip. Ported the PRD-01 `Button` and added a sibling `ButtonLink` (anchor styled with the same `.wm-btn` classes) for CTAs that navigate; ported the `.wm-btn` slice of the showcase `components.css` into `web/app/components/chrome.css` alongside all header/footer/nav styles (token-only, real pseudo-states).
- Files changed: web/app/components/Button.tsx (new), web/app/components/chrome.css (new), web/app/components/SiteHeader.tsx (new), web/app/components/SiteFooter.tsx (new), web/app/globals.css (@import chrome.css), web/app/layout.tsx (render SiteHeader/SiteFooter, body id="top"); prd.json (US-002 passes:true), progress.txt, checklist.md, PRDS.md.
- Checks run: `cd web && npm run typecheck` → no errors; `npm run lint` → No issues found; `npm run build` → compiled + 3 static pages, success. Token-literal audit on web/app/components + layout + globals → CLEAN (no hardcoded visual literals).
- Browser evidence: Playwright + system Chrome at http://localhost:3100, breakpoints 390/768/1280. Header: position `sticky`, top `0px`, z-index `50`, bg `rgb(255,255,255)`, boxShadow `none` (DESIGN invariant: no shadow on full-bleed chrome), borderBottom `1px rgb(204,204,204)` (silver hairline); header top stays `0` after scrolling 600px (pinned). Responsive swap: desktop `.wm-nav` display `none` @390 / `flex` @768+; `.wm-nav-toggle` `flex` @390 / `none` @768+. Mobile menu: closed by default, opens on toggle (`aria-expanded=true`), links [How it works, Examples, FAQ] present, CTA bg `rgb(0,0,0)` backgroundImage `none` color `rgb(255,255,255)` (black action system, no gradient), closes on link click. Footer: 10 links present (Product/Company/Social incl. Privacy, Terms, Contact, socials), boxShadow `none`. Screenshots: /tmp/wm-us002-{mobile,tablet,desktop}.png + /tmp/wm-us002-mobile-menu-open.png — clean, on-brand.
- Learnings for future iterations:
  - Global chrome belongs in `web/app/layout.tsx` (renders around `{children}`) so every page gets nav/footer; page files render only their own sections. Recorded the porting recipe (component TSX + its components.css slice → web/app/components/, @import from globals.css) in Codebase Patterns.
  - Components with interaction states MUST have their real CSS ported — inline `var()` can't express `:hover/:active/:focus-visible/:disabled`. Added `ButtonLink` (anchor + `.wm-btn` classes, `text-decoration:none`) so navigating CTAs share the exact Button styling without a `<button>`.
  - Wordmark uses Inter 700 (heading-sm), NOT Space Grotesk — the display face is banned below 26px per DESIGN.md; a ~18px nav wordmark would violate it.
  - Mobile nav = a `@media (max-width:767px)` CSS swap (hide desktop nav / show hamburger) + a `useState` toggle for the panel; keep the breakpoint logic in CSS, JS only owns open/closed.
---

## [2026-07-01 18:35] - US-003
- Implemented: Hero section (`web/app/components/Hero.tsx` + `hero.css`, rendered by `page.tsx`) leading with the brand hook headline (Space Grotesk display), an eyebrow Badge, a subhead naming the pain (your photos are costing you matches) + promise (pro dating photos from your own face, no photoshoot), an above-the-fold primary CTA ("Get my photos" → #waitlist) plus a ghost CTA ("See the transformations" → #examples), and the PRD-01 Before/After card as the credibility proof. Ported the transformation-proof stack from the design system into `web/app/components/` — `Badge`, `FeedPhotoTile` (rewired to `next/image` fill + `priority`/`sizes`, non-interactive div), `BeforeAfterCard` — with their CSS slice in `web/app/components/showcase.css`. Hero sits on the periwinkle→white wash surface, mobile-first single column → 2-col split at ≥768px.
- Real imagery: no licensed photos exist yet, so authored raster studio-portrait stand-ins (`web/public/hero/before.jpg` + `after.jpg`) via a committed generator `web/scripts/gen-hero-images.mjs` (sharp: SVG portrait → JPG; before = dull/cool/flat/soft-focus, after = warm/lit/styled). Served through next/image (real photographic-register raster, NOT a gradient placeholder), 3:4 so licensed photos swap in at the same paths with zero code change; honest "Sample transformation — real member results at launch" caption shown.
- Files changed: web/app/components/Badge.tsx (new), FeedPhotoTile.tsx (new), BeforeAfterCard.tsx (new), showcase.css (new), Hero.tsx (new), hero.css (new); web/scripts/gen-hero-images.mjs (new); web/public/hero/before.jpg + after.jpg (new); web/app/globals.css (@import showcase.css + hero.css), web/app/page.tsx (render <Hero/>); prd.json (US-003 passes:true), progress.txt, checklist.md, PRDS.md.
- Checks run: `cd web && npm run typecheck` → no errors; `npm run lint` → No issues found; `npm run build` → compiled + 3 static pages, success. Token-literal audit on all new component/CSS files → CLEAN (generator hex = baked image pixels, documented outside the token system).
- Browser evidence: Playwright + system Chrome at http://localhost:3100, breakpoints 390/768/1280. Title fontFamily "Space Grotesk", 36px @390 → 48px @768/1280, color rgb(0,0,0). Primary CTA text "Get my photos", href #waitlist, bg rgb(0,0,0), backgroundImage `none` (design invariant), color rgb(255,255,255), above the fold at all three widths. Ghost CTA "See the transformations" → #examples. Hero background = sky-periwinkle gradient (surface). BeforeAfterCard boxShadow = --shadow-sm; 2 next/image tiles loaded (naturalWidth 175/345/299 responsive), descriptive alt with before/after suffixes; "Before"/"After" pill labels present. SSR HTML emits `<link rel="preload" as="image">` for both before + after with responsive imageSrcSet/imageSizes (LCP preloaded); no CLS (fixed 3:4 aspect-ratio reserves space). Screenshots: /tmp/wm-us003-{mobile,tablet,desktop}.png.
- Learnings for future iterations:
  - Recorded the next/image port recipe + the self-authored hero-imagery approach in Codebase Patterns (see "Landing site imagery & PRD-01 component port"). US-005 gallery should reuse the FeedPhotoTile + gen-hero-images.mjs pattern.
  - The web FeedPhotoTile intentionally diverges from the showcase one (next/image fill, non-interactive div) — marketing tiles don't need the app's pressed/selected/loading button states, and next/image is required for optimization + LCP preload.
  - Real licensed before/after + testimonial photography remains an open question (imagery ownership) — US-005/US-007 will hit the same gap; the gen-hero-images.mjs stand-in pattern + honest caption is the interim answer until assets are provided.
---

## [2026-07-01 19:30] - US-004
- Implemented: "How it works" section (`web/app/components/HowItWorks.tsx` + `howitworks.css`, rendered by `page.tsx` after `<Hero/>`). `id="how-it-works"` matches the existing header + footer nav anchors, so those links now land here. Three steps rendered as a semantic `<ol>`: (1) Upload a few selfies, (2) Pick photos you love, (3) Get profile-ready shots. Each step is a DESIGN.md Standard Content Card (white surface, `--shadow-sm`, `--radius-cards`) holding a filled mono-weight inline-SVG icon (upload / heart / sparkle, `currentColor` = ink, 20px) on a soft ink-tint chip, a numbered `Step N` Badge (reused PRD-01 `Badge`), an Inter-700 title, and ONE tight sentence of slate body copy — exactly two free-text sizes per card. Section header uses an eyebrow Badge + Inter-700 24px title + a slate lede. Mobile-first single column → `repeat(3, 1fr)` at the ≥768px breakpoint. Section sits on the white canvas (rhythm against the hero's periwinkle wash) with no box-shadow.
- Files changed: web/app/components/HowItWorks.tsx (new), web/app/components/howitworks.css (new), web/app/globals.css (@import howitworks.css), web/app/page.tsx (render <HowItWorks/>); prd.json (US-004 passes:true + notes), progress.txt, checklist.md, PRDS.md.
- Checks run: `cd web && npm run typecheck` → no errors; `npm run lint` → No issues found; `npm run build` → compiled + 3 static pages, success. Token-literal audit on HowItWorks.tsx + howitworks.css → CLEAN (no hardcoded visual literals).
- Browser evidence: Playwright + system Chrome at http://localhost:3100, breakpoints 390/768/1280. Section bg rgb(255,255,255) (white canvas), boxShadow `none` (DESIGN invariant — no shadow on the section). 3 cards, each white bg + boxShadow `rgba(0,0,0,0.1) 0px 0px 6px 0px` (--shadow-sm) + borderRadius 12px (--radius-cards), each with 1 icon SVG + a `Step 1/2/3` Badge + a title + one sentence (108/128/123 chars). Grid: 1 track @390 (single column) → `repeat(3,1fr)` @768 (224px×3) and @1280 (368px×3). Section title fontFamily "Inter…" (NOT Space Grotesk), 24px, rgb(0,0,0). Screenshots: /tmp/wm-us004-{390,768,1280}.png — clean, on-brand.
- Learnings for future iterations:
  - Section anchors are already wired in the nav (US-002): the header/footer link to `#how-it-works` / `#examples` / `#faq`. When building a section, set the matching `id` on the `<section>` so those links work — no header change needed. US-005 must use `id="examples"`, US-009 `id="faq"`, US-010 `id="waitlist"`.
  - Section titles below the hero use **Inter 700** (heading 24px), never the Space Grotesk display face — DESIGN.md reserves the display face for hero statements ≥26px. Keep the eyebrow-Badge + Inter-700-title + slate-lede header pattern for the remaining sections for visual consistency.
  - Filled mono inline-SVG icons (`fill="currentColor"`, ink) drawn at 20px on a `--surface-tint-soft` chip satisfy DESIGN.md's "filled mono-weight shapes, only #000/#fff" imagery rule and stay token-only (the icon color is a token via currentColor). Reuse this icon-chip pattern for benefits (US-006) / FAQ.
---

## [2026-07-01 20:20] - US-005
- Implemented: Transformation gallery / sample feed showcase (`web/app/components/Gallery.tsx` + `gallery.css`, rendered by `page.tsx` after `<HowItWorks/>`). `id="examples"` matches the existing header + footer "Examples" nav anchor and the hero's "See the transformations" CTA (both already target `#examples`). Eight curated sample RESULT tiles rendered with the ported PRD-01 `FeedPhotoTile` — the same component that drives the app's feed — so the marketing gallery mirrors the app's feel. Each tile carries a category chip (Travel / Gym / Professional / Night out, two each) via the tile's `category` prop → `Badge onPhoto` over the legibility scrim, to communicate range. Section header reuses the established eyebrow-Badge + Inter-700-title + slate-lede pattern; an honest "Sample transformations — real member results at launch" caption sits below the grid. Grid is mobile-first: 2 columns → 3 at ≥768px → 4 at ≥1024px. Section sits on the `--gradient-sky-periwinkle` wash (surface only) for the DESIGN.md alternating white/wash rhythm; no box-shadow on the section.
- Real imagery: no licensed photos yet, so authored raster category stand-ins (`web/public/gallery/{travel,gym,professional,night-out}-{1,2}.jpg`) via a new committed generator `web/scripts/gen-gallery-images.mjs` that reuses the `gen-hero-images.mjs` portrait pattern — one background palette + key light + wardrobe per category, varied `people` presets so the set reads as different faces. Served through next/image (real photographic-register raster, NOT gradient placeholders), 3:4 so licensed photos swap in at the same paths with zero code change.
- Lazy + no-jank: gallery tiles pass NO `priority`, so next/image lazy-loads them (`loading="lazy"`) and they are NOT preloaded in SSR (opposite of the hero's LCP tiles); the fixed 3:4 `aspect-ratio` on `.wm-tile` reserves space so lazy decode causes no CLS.
- Files changed: web/app/components/Gallery.tsx (new), web/app/components/gallery.css (new), web/scripts/gen-gallery-images.mjs (new), web/public/gallery/*.jpg (8 new); web/app/globals.css (@import gallery.css), web/app/page.tsx (render <Gallery/>); prd.json (US-005 passes:true + notes), progress.txt, checklist.md, PRDS.md.
- Checks run: `cd web && npm run typecheck` → no errors; `npm run lint` → No issues found; `npm run build` → compiled + 3 static pages, success. Token-literal audit on Gallery.tsx + gallery.css + page.tsx → CLEAN (generator hex = baked image pixels, documented outside the token system).
- Browser evidence: Playwright + system Chrome at http://localhost:3100, breakpoints 390/768/1280. Section bg = sky-periwinkle gradient wash, boxShadow `none`. Grid columns: 2 @390 → 3 @768 → 4 @1280. 8 tiles, 8/8 next/image tiles loaded after scroll into view, 8/8 with `loading="lazy"`; `curl … | grep 'rel="preload"'` lists NO gallery images (lazy, not preloaded). Category chips present in order Travel/Gym/Professional/Night out ×2. Tile aspect 1.333 (= 3:4, no CLS). Section title fontFamily "Inter…" (NOT Space Grotesk), 24px. Screenshots: /tmp/wm-us005-{mobile,tablet,desktop}.png — clean, on-brand, mirrors the app feed.
- Learnings for future iterations:
  - Recorded the feed-gallery + lazy-imagery pattern and the `gen-gallery-images.mjs` category-generator in Codebase Patterns. US-007 testimonial avatars will hit the same real-imagery gap — reuse the generator pattern + honest caption until licensed assets land.
  - Gallery/below-fold tiles must NOT pass `priority` (that's for the hero LCP only) — lazy-load + fixed aspect-ratio is the no-jank recipe; verify with the `rel="preload"` curl grep returning no gallery entries.
---
